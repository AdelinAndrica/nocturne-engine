---
/*
 * Design choice (not directly from the book):
 * Nocturne's public documentation uses the same locked dark product palette
 * as the editor baseline, so the generic Starlight light/dark selector is omitted.
 */
---
